<Window x:Class="Translator.Desktop.ShortcutSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Screenshot shortcut" Height="250" Width="420"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="21" FontWeight="SemiBold" Text="Screenshot shortcut"/>
        <TextBlock Grid.Row="1" Margin="0,7,0,16" Foreground="#52635A"
                   TextWrapping="Wrap"
                   Text="Choose a combination that is not used by another application."/>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <ComboBox x:Name="ModifiersBox" Width="190" Height="36"/>
            <TextBlock Margin="10,7" FontWeight="SemiBold" Text="+"/>
            <ComboBox x:Name="KeyBox" Width="90" Height="36"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Height="36" IsCancel="True" Content="Cancel"/>
            <Button Margin="10,0,0,0" Width="110" Height="36" IsDefault="True"
                    Click="Save_Click" Content="Save"/>
        </StackPanel>
    </Grid>
</Window>
